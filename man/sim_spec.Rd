% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specifications.r
\name{sim_spec}
\alias{sim_spec}
\title{Simulation Specification Constructor}
\usage{
sim_spec(
  name = "Setup0",
  n_subj = 10,
  n_obs = 5,
  p = NULL,
  q = 1,
  beta_coeff = NULL,
  SNR = NULL,
  X = NULL,
  Z = NULL,
  is.CorrelatedXZ = FALSE,
  is.ZInX = FALSE,
  orthogonalize.X = FALSE,
  orthogonalize.Z = FALSE,
  include.Xintercept = TRUE,
  include.Zintercept = TRUE,
  ...
)
}
\arguments{
\item{name}{Character. Label for the simulation setup. Default: \code{"Setup0"}.}

\item{n_subj}{Integer (>0). Number of subjects or clusters. Default: \code{10}.}

\item{n_obs}{Integer scalar or integer vector (>0).
Specifies the number of observations per subject/cluster.
\itemize{
\item If a \strong{scalar} is supplied, all \code{n_subj} clusters are assigned the same number of observations.
\item If a \strong{vector}, it must be of length \code{n_subj}, with each entry giving the size of the respective cluster.
Useful for longitudinal or clustered designs.  Default: \code{5} for all clusters.
}}

\item{p}{Integer (>0). Number of fixed-effect predictors (including intercept).
If \code{NULL}, it is inferred as \code{length(beta_coeff)} whenever possible.
Default fallback value: \code{1}}

\item{q}{Integer (>0). Number of random-effect predictors. Default: \code{1}.}

\item{beta_coeff}{Numeric vector. Fixed-effect coefficients of length \code{p}.
If supplied, its length is used to infer \code{p} when \code{p = NULL}.}

\item{SNR}{Numeric (>0). Signal-to-noise ratio.}

\item{X}{Numeric matrix or list. User-supplied design matrix for fixed effects.
If provided, it is stored verbatim.}

\item{Z}{Numeric matrix or list. User-supplied design matrix for random effects.
If provided, it is stored verbatim.}

\item{is.CorrelatedXZ}{Logical. Whether \code{X} and \code{Z} are correlated
(and hence they need to be generated jointly). Default: \code{FALSE}.}

\item{is.ZInX}{Logical. Whether the columns of \code{Z} are the same as
the initial columns of \code{X}. Default: \code{FALSE}.}

\item{orthogonalize.X}{Logical. Whether orthogonalize (non-intercept) columns
of \code{X} after construction. Default: \code{FALSE}.}

\item{orthogonalize.Z}{Logical. Whether orthogonalize (non-intercept) columns
of \code{Z} after construction. Default: \code{FALSE}.}

\item{include.Xintercept}{Logical. Whether an intercept column should be included in \code{X}. Default: \code{TRUE}.}

\item{include.Zintercept}{Logical. Whether an intercept column should be included in \code{Z}. Default: \code{TRUE}.}

\item{...}{Additional named arguments to append to the returned specification list.

#' @return
A named list containing:
\itemize{
\item \code{name} — Setup label.
\item \code{n_subj}, \code{n_obs} — Sample size arguments.
\item \code{p}, \code{q} — Predictor dimensions (as supplied or inferred).
\item \code{beta_coeff} — Fixed-effect coefficients.
\item \code{SNR} - Desired signal-to-noise ratio.
\item \code{X}, \code{Z} — Optional user-supplied design matrices.
\item \code{is.CorrelatedXZ}, \code{is.ZInX} — Logical flags,
specifying desired relationships between the design matrices \code{X} and \code{Z} .
\item \code{orthogonalize.X}, \code{orthogonalize.Z} — Orthogonalization flags.
\item \code{include.Xintercept}, \code{include.Zintercept} — Intercept controls.
\item Any additional entries from \code{...}.
}}
}
\description{
Constructs a unified simulation specification object (list) for generating data
from several structured statistical models, e.g., linear models (LM),
generalized linear models (GLM), linear mixed models (LMM),
generalized LMMs (GLMM), or other regression-type models.
The output may directly be passed to the corresponding simulator functions,
such as \code{\link{simulate_LMdata}}, \code{\link{simulate_LMMdata}}, etc.
}
\examples{
# Basic specification
spec1 <- sim_spec()

# Custom fixed effects
spec2 <- sim_spec(beta_coeff = c(1, -1, 0.5))

# Mixed-effects style specification
spec3 <- sim_spec(
  n_subj = 15, n_obs = 6,
  beta_coeff = c(1, 0.5),
)

}
\seealso{
\code{\link{simulate_LMdata}},
\code{\link{simulate_LMMdata}}
}
