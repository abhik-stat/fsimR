% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_methods.r
\name{summarySimData}
\alias{summarySimData}
\alias{summary.LMMdata}
\alias{summary.GLMMdata}
\alias{summary.LMdata}
\alias{summary.GLMdata}
\alias{summary.IIDdata}
\alias{summary.list}
\title{Summarize Simulated Data}
\usage{
\method{summary}{LMMdata}(object, replication = 1, iteration = 1, ...)

\method{summary}{GLMMdata}(object, replication = 1, iteration = 1, ...)

\method{summary}{LMdata}(object, replication = 1, iteration = 1, ...)

\method{summary}{GLMdata}(object, replication = 1, iteration = 1, ...)

\method{summary}{IIDdata}(object, ...)

\method{summary}{list}(object, replication = 1, iteration = 1, ...)
}
\arguments{
\item{object}{Numeric vector, matrix, named list, or simulation object
(of class \code{LMMdata}, \code{GLMMdata}, \code{LMdata}, \code{GLMdata}, \code{IIDdata}).
If a list, it must include the components required for the selected \code{type}.}

\item{replication}{Integer; which replication (dataset) to plot
(LMM/GLMM, LM/GLM only; default \code{1}).}

\item{iteration}{Integer; which iteration within the replication to plot
(LMM/GLMM, LM/GLM only; default \code{1}).}

\item{...}{Any additional arguments.}
}
\value{
Invisibly returns a numeric matrix of computed summary statistics:
\itemize{
\item \strong{Rows:} variables present in the object (e.g., \code{y}, \code{X}, \code{Z}, \code{RE}).
\item \strong{Columns:} \code{Mean}, \code{SD}, \code{Min}, \code{Q1}, \code{Median}, \code{Q3}, \code{Max}, and total count \code{n}.
}
}
\description{
Computes and prints a comprehensive summary for simulated datasets,
supporting multiple types:
\itemize{
\item \strong{Mixed Models} (of class \code{LMMdata} or \code{GLMMdata})
\item \strong{Regression Models} (of class \code{LMdata} or \code{GLMdata})
\item \strong{IID samples} (of class \code{IIDdata})
}

The function also works for named lists containing:
\itemize{
\item response vectors as \code{y}
\item fixed-effects design matrix as \code{X}
\item optional random-effects design matrix as \code{Z}
\item optional random-effects realizations as \code{RE}
}

Numeric vectors or matrices are treated as IID samples (columns = variables).

For details of summary statistics, see Details.
}
\details{
The function prints a comprehensive summary for the simulated dataset:
\itemize{
\item Simulation metadata such as number of subjects,
observation counts, dimensions of \code{X}, \code{Z}, \code{RE} (for LMM/GLMM),
fixed-effects coefficients, signal-to-noise ratio (\code{SNR}),
and residual standard deviation (\code{sigma_e}).
\item Optional distribution names if \code{distr_settings} is provided within object.
\item Standard summary statistics (Mean, SD, Min, Q1, Median, Q3, Max)
and total number of observations (\code{n}) for all variables in the object
(\code{y}, \code{X}, \code{Z}, \code{RE} for mixed models; \code{y}, \code{X} for regression models;
all columns for IID samples) for the specified \code{replication} and \code{iteration}.
\item Correlation matrices for \code{X}, \code{Z}, and \code{RE} (if present),
excluding intercept or any constant columns.
}
}
\examples{
\dontrun{
# Mixed Model
sim_settings <- sim_spec(p = 5)
lmm_data <- simulate_LMMdata(c(2,3), sim_settings)
summary(lmm_data, replication = 1, iteration = 1)

# Regression Model
lm_data <- simulate_LMdata(c(2,3), sim_settings)
summary(lm_data, replication = 1, iteration = 1)

# IID Samples (numeric matrix)
iid_data <- matrix(rnorm(100), ncol = 5)
summary(iid_data)

# Named list with components like LMM
mylist <- list(
  y = lmm_data$y,
  X = lmm_data$X,
  Z = lmm_data$Z,
  RE = lmm_data$RE
)
summary(mylist, replication = 1, iteration = 1)
}

}
