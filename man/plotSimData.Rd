% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_methods.r
\name{plotSimData}
\alias{plotSimData}
\alias{plot.LMMdata}
\alias{plot.GLMMdata}
\alias{plot.LMdata}
\alias{plot.GLMdata}
\alias{plot.IIDdata}
\alias{plot.list}
\title{Plot Simulated Data}
\usage{
\method{plot}{LMMdata}(
  x,
  replication = 1,
  iteration = 1,
  type = c("all", "y", "X", "Z", "RE"),
  main = TRUE,
  plot_args = list(),
  ...
)

\method{plot}{GLMMdata}(x, ...)

\method{plot}{LMdata}(
  x,
  replication = 1,
  iteration = 1,
  type = c("all", "y", "X"),
  main = TRUE,
  plot_args = list(),
  ...
)

\method{plot}{GLMdata}(x, ...)

\method{plot}{IIDdata}(x, main = TRUE, plot_args = list(), ...)

\method{plot}{list}(x, ...)
}
\arguments{
\item{x}{Numeric vector, matrix, named list, or simulation object
(of class \code{LMMdata}, \code{GLMMdata}, \code{LMdata}, \code{GLMdata}, \code{IIDdata}).
If a list, it must include the components required for the selected \code{type}.}

\item{replication}{Integer; which replication (dataset) to plot
(LMM/GLMM, LM/GLM only; default \code{1}).}

\item{iteration}{Integer; which iteration within the replication to plot
(LMM/GLMM, LM/GLM only; default \code{1}).}

\item{type}{Character vector specifying which components to plot:
\itemize{
\item \code{"y"}: response vector
\item \code{"X"}: fixed-effects or design matrix
\item \code{"Z"}: random-effects design matrix (LMM/GLMM only)
\item \code{"RE"}: random effects (LMM/GLMM only)
\item \code{"all"}: all available components (default)
}

Ignored for IID samples, where all columns are always plotted.}

\item{main}{Logical; if \code{TRUE}, descriptive titles are shown (default \code{TRUE}).}

\item{plot_args}{Optional list of graphical parameters to customize
appearance of the plots. Supported arguments:
\describe{
\item{breaks}{Number of histogram bins (default \code{10})}
\item{hist_col}{Histogram fill color (default \code{"lightsteelblue"})}
\item{dens_col}{Line color for density curve (default \code{"firebrick"})}
\item{dens_lwd}{Line width for density curve (default \code{2})}
\item{smooth_col}{Smoothing line color (default \code{"blue"})}
\item{smooth_lwd}{Smoothing line width (default \code{2})}
\item{cex_axis}{Character expansion for axis labels (default \code{1.4})}
\item{label_line}{Line offset for margin labels (default \code{0})}
\item{low_col}{Background color for \code{r = 0} in correlation panels (default \code{"#FFFFFF"})}
\item{high_col}{Background color for \verb{|r| = 1} in correlation panels (default \code{"#2166AC"})}
\item{cex_text}{Character expansion for correlation/p-value text (default \code{1.4})}
\item{digits}{Number of digits for correlation/p-value (default \code{4})}.
}}

\item{...}{Additional graphical arguments passed to
the standard plotting functions, such as \code{hist}, \code{lines}, \code{pairs} and \code{text}
(via internal functions \code{.plot_hist_density} and \code{.plot_pairs()}).}
}
\value{
Invisibly returns \code{NULL}. Generates plots for visual inspection.
}
\description{
Generates visualizations for simulated datasets, supporting multiple types:
\itemize{
\item \strong{Mixed Models} (of class \code{LMMdata} or \code{GLMMdata})
\item \strong{Regression Models} (of class \code{LMdata} or \code{GLMdata})
\item \strong{IID samples} (of class \code{IIDdata})
}

The function also works for named lists containing:
\itemize{
\item response vectors as \code{y}
\item fixed-effects design matrix as \code{X}
\item optional random-effects design matrix as \code{Z}
\item optional random-effects realizations as \code{RE}
}

Numeric vectors or matrices are treated as IID samples
where columns correspond to variables, rows to observations.

For plot types and layout, see Details.
}
\details{
The plotting behavior depends on the class and selected components (in \code{type}):
\enumerate{
\item \strong{Histograms with density overlay} for response vectors (\code{y})
or any numeric vector (treated as a univariate IID samples).
Skipped for constant vectors.
\item \strong{Enhanced pairs plots} for matrices (\code{X}, \code{Z}, \code{RE}) or multivariate IID samples:
\itemize{
\item Diagonal panels: histograms with density scaled to \code{[0,1]}.
\item Upper panels: scatter plots with smoothing lines (\link{loess}).
\item Lower panels: correlation coefficients, p-values,
and significance stars (\code{*}, \verb{**}, \verb{***}).
Background color reflects the absolute correlation;
negative correlations appear in red.
\item Constant columns are automatically removed.
}
}
}
\examples{
\dontrun{
# Mixed Model
sim_settings <- sim_spec(p = 3)
sim_data <- simulate_LMMdata(c(2,3), sim_settings)
plot(sim_data, type = c("y", "RE"))

# Regression Model
sim_data <- simulate_LMdata(c(2,3), sim_settings)
plot(sim_data, type = "X")

# IID data
sim_data <- simulate_IIDdata(n = 100)
plot(sim_data)

# Named list wrapper
mylist <- list(y = rnorm(50), X = matrix(rnorm(100), ncol=2))
plot(mylist)

}

}
